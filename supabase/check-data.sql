-- =====================================================================
-- ตรวจว่าข้อมูลตัวอย่างเข้าฐานข้อมูลครบ (รันหลัง schema.sql และ seed.sql)
-- ผลที่ถูกต้อง: 8 แถว ตั้งแต่ CMP-01 ถึง ROB-01
-- =====================================================================

select machine_code, name, status from machines order by machine_code;
